services:
  nginx:
    image: nginx:alpine
    ports:
      - "9999:9999"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - sock:/run/sock
    depends_on:
      - api1
      - api2
    deploy:
      resources:
        limits:
          cpus: "0.2"
          memory: "30MB"
    networks:
      - rinha

  api1:
    image: ghcr.io/joseiedo/rinha-de-backend-2026:0.0.2
    platform: linux/amd64
    environment:
      INDEX_PATH: /app/resources/index.bin
      SOCKET_PATH: /run/sock/api1.sock
    volumes:
      - sock:/run/sock
    ulimits:
      nofile: 65535
    deploy:
      resources:
        limits:
          cpus: "0.40"
          memory: "155MB"
    networks:
      - rinha

  api2:
    image: ghcr.io/joseiedo/rinha-de-backend-2026:0.0.2
    platform: linux/amd64
    environment:
      INDEX_PATH: /app/resources/index.bin
      SOCKET_PATH: /run/sock/api2.sock
    volumes:
      - sock:/run/sock
    ulimits:
      nofile: 65535
    deploy:
      resources:
        limits:
          cpus: "0.40"
          memory: "155MB"
    networks:
      - rinha

volumes:
  sock:
    driver: local
    driver_opts:
      type: tmpfs
      device: tmpfs
      o: "size=4m,mode=0777"

networks:
  rinha:
    driver: bridge
